# Use the official NGINX image as the base
FROM nginx:alpine

# Copy the custom NGINX configuration file into the container
COPY nginx.conf /etc/nginx/nginx.conf

# Expose the port used by NGINX (optional, for documentation)
EXPOSE 5000